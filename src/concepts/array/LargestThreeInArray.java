package concepts.array;

public class LargestThreeInArray
{
    /*
     * Find the largest three elements in an array Given an array with all distinct elements, find the largest three elements.
     * 
     * Expected time complexity is O(n)
     * 
     * Input: arr[] = {10, 4, 3, 50, 23, 90} Output: 90, 50, 23
     */

    public static void main(String[] args)
    {
	int largest, secLarg, thirdLarg, temp;

	int[] arr = { 10, 4, 3, 50, 23, 90 };

	largest = arr[0];

	secLarg = arr[1];

	thirdLarg = arr[2];
	
//	if(largest > secLarg && )

	for (int i = 0; i < arr.length; i++)
	{

	}
    }

}
